/*using UnityEngine;

public class TankEnemy : EnemyController
{
    protected override void Awake()
    {
        base.Awake();
        moveSpeed = 1.8f;
        contactDamage = 25;
        if (health != null)
            health.SetMaxHealth(200); // asume que tu componente Health tiene esto
    }
}
*/