/*using UnityEngine;

public class FastEnemy : EnemyController
{
    protected override void Awake()
    {
        base.Awake();
        moveSpeed = 6f;
        contactDamage = 8;
        if (health != null)
            health.SetMaxHealth(50);
    }
}
*/